---
  - name: Initial setup
    hosts: all
    roles:
      - init

  - name: Database server
    hosts: db_servers
    roles:
      - prometheus
      - mysql
      - bind

  - name: Web server
    hosts: web_servers
    roles:
      - agama
      - uwsgi
      - nginx
